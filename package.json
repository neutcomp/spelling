{
    "name": "spelling-app",
    "version": "1.0.2",
    "description": "The spelling package file",
    "scripts": {
        "start": "http-server . -p 8000 -c-1 --silent",
        "test": "cypress run --config 'baseUrl=http://127.0.0.1:8000'",
        "precommit": "start-server-and-test 8000",
        "spelling:prod": "git ftp push",
        "cypress:open": "cypress open"
    },
    "devDependencies": {
        "@angular/cli": "^9.1.1",
        "husky": "^4.2.5",
        "start-server-and-test": "^1.11.0"
    },
    "dependencies": {
        "cypress": "^4.12.1"
    }
}
